---
import Layout from '@layouts/Layout.astro'
import eslintConfig from '@root/eslint.config?raw'
import prettierConfig from '@root/.prettierrc?raw'
import tsconfig from '@root/tsconfig.json?raw'
import packageJson from '@root/package.json?raw'
import astroConfig from '@root/astro.config.mjs?raw'
import mise from '@root/mise.toml?raw'
import globalCss from '@root/src/styles/global.css?raw'
import type { CodeLanguage, GetStaticPaths } from 'astro'
import { Code } from 'astro:components'
import { geti18nStaticPaths } from '@root/src/i18n'

const { t } = Astro.locals

interface StackItem {
  type: string
  name: string
  href: string
}

const stack = [
  { type: t('aboutPage.stack.framework'), name: 'Astro (SSG)', href: 'https://astro.build/' },
  {
    type: t('aboutPage.stack.styleEngine'),
    name: 'Tailwind CSS',
    href: 'https://tailwindcss.com/',
  },
  {
    type: t('aboutPage.stack.scripts'),
    name: 'TypeScript',
    href: 'https://www.typescriptlang.org/',
  },
  { type: t('aboutPage.stack.packageManager'), name: 'Bun', href: 'https://bun.com/' },
  { type: t('aboutPage.stack.linter'), name: 'ESLint', href: 'https://eslint.org/' },
  { type: t('aboutPage.stack.formatter'), name: 'Prettier', href: 'https://prettier.io/' },
  {
    type: t('aboutPage.stack.aiEditor'),
    name: 'Opencode (MiMo V2.5 Free)',
    href: 'https://opencode.ai/',
  },
] as const satisfies StackItem[]

interface ConfigItem {
  name: string
  code: string
  type: CodeLanguage
}

const configs = [
  {
    name: 'astro.config.mjs',
    code: astroConfig,
    type: 'js',
  },
  {
    name: 'eslint.config.js',
    code: eslintConfig,
    type: 'ts',
  },
  {
    name: '.prettierrc',
    code: prettierConfig,
    type: 'json',
  },
  {
    name: 'tsconfig.json',
    code: tsconfig,
    type: 'jsonc',
  },
  {
    name: 'package.json',
    code: packageJson,
    type: 'json',
  },
  {
    name: 'src/styles/global.css',
    code: globalCss,
    type: 'css',
  },
  {
    name: 'mise.toml',
    code: mise,
    type: 'toml',
  },
] as const satisfies ConfigItem[]

export const getStaticPaths = (() => {
  return geti18nStaticPaths()
}) satisfies GetStaticPaths
---

<Layout parentHref="/o-witrynie" title={t('aboutPage.title')}>
  <section>
    <header class="mb-6 text-4xl font-semibold text-center">
      <h1>{t('aboutPage.title')}</h1>
    </header>
    <article>
      <header class="mb-4 text-2xl">
        <h2>{t('aboutPage.techStack')}</h2>
      </header>
      <table>
        {
          stack.map((item) => (
            <tr>
              <td class="py-1 pr-4">{item.type}:</td>
              <td>
                <a
                  class="text-blue-500 border-b-2 border-transparent border-solid visited:text-purple-500 hover:border-b-2 hover:border-blue-500 hover:border-solid hover:visited:border-b-2 hover:visited:border-solid hover:visited:border-purple-500"
                  href={item.href}
                  target="_blank"
                >
                  {item.name}
                </a>
              </td>
            </tr>
          ))
        }
      </table>
    </article>
    <article class="mt-4">
      <header class="mb-4 text-2xl">
        <h2>{t('aboutPage.configs')}</h2>
      </header>
      <div class="flex flex-col gap-2">
        {
          configs.map((item) => (
            <div class="overflow-hidden min-h-0">
              <input
                type="checkbox"
                id={`toggle-${item.name}`}
                hidden
                class="peer"
              />
              <label
                for={`toggle-${item.name}`}
                class="flex flex-row items-center border-2 border-black"
              >
                <p class="flex-1 p-2 border-r-2 border-black">{item.name}</p>
                <span class="p-2 px-4 transition-transform rotate-90">
                  &rangle;
                </span>
              </label>

              <div class="grid overflow-hidden ease-in-out duration-400 grid-rows-[0fr] transition-[grid-template-rows] peer-checked:grid-rows-[1fr]">
                <div class="min-h-0">
                  <Code
                    class="p-4"
                    data-name={item.name}
                    code={item.code}
                    lang={item.type}
                  />
                </div>
              </div>
            </div>
          ))
        }
      </div>
    </article>
  </section>
</Layout>

<style>
  input.peer:checked + label > span {
    scale: -1 -1;
  }
</style>
