---
title: 'Dubbingowanie Skyrima #4'
subtitle: 'Generowanie plików lip'
description: 'Zapoznaj się z tym, jak stworzyć plik lip poza Creation Kit za pomocą FaceFXWrapper, FFmpega i Pythona'
date: '2026-08-15'
tags: ['Dubbing', 'Skyrim', 'Modding']
---

import Directory from '@components/DirectoryView/Directory.astro'
import Spoiler from '@components/Spoiler.astro'
import CodeVariant from '@components/CodeVariant.astro'
import generateLipLinux from './scripts/skyrim_lip/generate_lip_linux.py?raw'
import generateLipWindows from './scripts/skyrim_lip/generate_lip_windows.py?raw'
import fullScriptLinux from './scripts/skyrim_lip/full_script_linux.py?raw'
import fullScriptWindows from './scripts/skyrim_lip/full_script_windows.py?raw'

## Wstęp

Witam w trzecim wpisie, w którym pokrywam temat dubbingowania modyfikacji do Skyrima.

Tutaj skupię się na tym, jak stworzyć plik `.lip` poza Creation Kit za pomocą FaceFXWrapper i Pythona.

Tutaj, poza FaceFXWrapperem, wykorzystany będzie też FFmpeg do wstępnego przetwarzania audio do generowania pliku `.lip`.

## Narzędzia

### FFmpeg

**FFmpeg** jest potężnym narzędziem CLI, które umożliwia przetwarzanie i konwersję pliku audio i wideo.

#### Instalacja

Na Linuxie można zainstalować FFmpeg poprzez instalator dystrybucji (tj. `dnf` czy `apt`).

Na Windowsie jest to bardziej skomplikowane, stąd zamieszczam [link do stackexchange.com](https://video.stackexchange.com/questions/20495/how-do-i-set-up-and-use-ffmpeg-in-windows), w którym jest rozwiązanie.

#### Wykorzystanie

W obecnym przypadku będziemy chcieli przekonwertować plik audio na 16-bitowy PCM, jednokanałowy plik `.wav` o częstotliwości 16 kHz.

W trakcie prac nad dubbingiem do `Gifts of the Outsider` wyszło, że wyższa częstotliwość audio używanego do generowania pliku `.lip` powoduje desynchronizację ruchu ust z mową a rozwiązaniem jest stworzenie tymczasowego pliku o niższej częstotliwości.

#### Komenda

```bash
ffmpeg
  -y              # Zezwól na nadpisanie pliku
  -i source       # ścieżka do pliku wejściowego
  -ac 1           # ilość kanałów audio pliku wyjściowego
  -ar 16000       # częstotliwość pliku wyjściowego
  -c:a pcm_s16le  # format pliku wyjściowego [znakowany 16-bitowy PCM Little Endian]
  temp_path       # ścieżka do pliku wyjściowego

```

### FaceFXWrapper

**FaceFXWrapper** to narzędzie CLI, które umożliwia generowanie plików `.lip` bez wchodzenia do CreationKit.

Do działania wymaga FonixData.cdf.

Teoretycznie umożliwia resampling, ale wykrzacza się, gdy dzwięk jego wielokanałowy, stąd preferuję FFmpega zastosować przetworzenia audio.

[FaceFXWrapper (Nexus Mods)](https://www.nexusmods.com/skyrimspecialedition/mods/20061?tab=files)

[FaceFXWrapper (GitHub)](https://github.com/Nukem9/FaceFXWrapper)

[Link do FonixData.cdf](https://www.nexusmods.com/skyrimspecialedition/mods/40971?tab=files)

#### Komenda

```bash
FaceFXWrapper [Type] [Lang] [FonixDataPath] [WavPath] [LipPath] [Text]
```

- **Type** - Fallout4, Skyrim
- **Lang** - USEnglish
- **FonixDataPath** - sciezka do FonixData.cdf
- **WavPath** - ścieżka do przetworzonego pliku `.wav`
- **LipPath** - sciezka do wyjściowego pliku `.lip`
- **Text** - tekst bazowy

### Python

**Python** to popularny język skryptowy nazwany po brytyjskiej grupie komików `Monty Python`.

W naszym przypadku będzie narzędziem integrującym plik z tekstami, FFmpeg oraz FaceFXWrapper.

Na Linuxie przeważnie dochodzi preinstalowany lub jest dostępny w menedżerze pakietów dystrybucji (ostatecznie zostaje instalacja poprzez `uv` czy `mise`).

Na Windowsie trzeba go pobrać przez instalator dostępny na [Python.org](https://www.python.org/downloads/) (sugeruję pobrać najnowszą dostępną).

Tutaj wykorzystam wersję 3.14.6, jednak nowsze czy nieco starsze (weźmy 3.10 i nowsze) powinny też działać.

#### Komenda

Uruchomienie skryptu w pliku `main.py` (nazwa dowolna, byleby plik ze skryptem był dostępny)

```bash
python main.py
```

## Zakładana struktura plików

Folder `bsa-output` to wynik rozpakowania pliku `.bsa`, który zawiera pliki `.wav`.

Plik `texts.csv` będzie zawierać informacje o tym, jaki tekst jest przypisany do pliku audio, by potem odczytać ze skryptu co trzeba.

Sam skrypt będzie wyszukiwać rekurencyjnie `bsa-output` co oznacza, że niezależnie od struktury tego folderu, on znajdzie pliki `.wav`.

<Directory
  name="."
  items={[
    {
      name: 'tools',
      items: [
        {
          name: 'FaceFXWrapper.exe',
          type: 'file',
        },

        {
          name: 'FonixData.cdf',
          type: 'file',
        },
      ],
    },
    {
      name: 'bsa-output',
      items: [
        {
          name: 'voice',
          items: [
            {
              name: '<nazwa-pluginu>',
              items: [
                {
                  name: '<nazwa-voice-type>',
                  items: [
                    {
                      name: 'file_1.wav',
                      type: 'file',
                    },
                    {
                      name: '<...pliki wav...>',
                      type: 'file',
                    },
                  ],
                },
              ],
            },
          ],
        },
      ],
    },
    {
      name: 'gen_lips.py',
      type: 'file',
    },
    {
      name: 'texts.csv',
      type: 'file',
    },

]}
/>

### texts.csv

**csv** - Jest to plik, który zawiera informacje tabelaryczne rozdzielane przecinkiem, który można otwierać w Excelu czy LibreOffice Calc.

Praktycznie może to być dowolny separator (zastosuję tutaj średnik).

#### Zakładana zawartość

```csv
text;wav;voiceType
"Ala ma kota";"plik1.wav";male_nord
"Kot ma alę";"plik2.wav";male_nord
"Ala ma kota";"plik1.wav";female_nord
"Kot ma alę";"plik2.wav";female_nord
```

## Skrypty

<div class="space-y-2">
  <Spoiler name="Deklaracja ścieżek">

```py
# pathlib znacząca ułatwia pracę ze ścieżkami
from pathlib import Path

"""
Tutaj rglob szuka w folderze "bsa-output"
wszystkich plików z rozszerzeniem ".wav"
"""
wavs_path = Path("bsa-output").rglob("*.wav")
facefx_wrapper = Path("tools", "FaceFXWrapper.exe")
fonix_data = Path("tools", "FonixData.cdf")
source_csv = Path("texts.csv")
```

  </Spoiler>

  <Spoiler name="Funkcja szykująca plik do lip">

```py
from subprocess import run, DEVNULL

def prepare_wav(source: Path) -> Path:
    temp_path = source.with_name(f"{source.stem}.temp.wav")

    cmd = f"ffmpeg -y -i {source} -ac 1 -ar 16000 -c:a pcm_s16le {temp_path}"

    result = run(
        cmd,
        # Umozliwia wklejenie całej komendy w formie tekstu bez bawienia się w tablice argumentów
        shell=True,
        # Usuwa komunikaty z ffmpega
        # Jak coś nie działa, to usunąć parametry stdout i stderr
        stdout=DEVNULL,
        stderr=DEVNULL,
    )

    if result.returncode != 0:
        raise Exception(f"Failed to convert {source}")

    # Będziemy tego potrzebować w trakcie generowania pliku lip
    return temp_path
```

  </Spoiler>
  <Spoiler name="Funkcja generująca plik lip">

<CodeVariant
  items={[
    { code: generateLipLinux, lang: 'py', name: 'Linux' },
    { code: generateLipWindows, lang: 'py', name: 'Windows' },
  ]}
/>

</Spoiler>

<Spoiler name="Klasa umożliwiająca odczyt pliku CSV z funkcją szukania potrzebnych danych">

```py
from csv import DictReader
from typing import TypedDict

# Ta klasa jest pod to,
# by edytor z rozszerzeniem do Pythona
# mógł podpowiadać pola i krzyczeć,
# gdy próbujemy odnieść się do nieistniejącego pola
class ItemType(TypedDict):
    wav: str
    text: str
    voiceType: str


class Reader:
    data: list[ItemType]
    source_csv: Path

    def __init__(self, source_csv: Path) -> None:
        self.source_csv = source_csv
        with open(source_csv, "r") as f:
            self.data = [*DictReader(f)]

    def find_by_file_name(self, voice_type: str, file_name: str) -> ItemType | None:
        for item in self.data:
            if item["voiceType"] == voice_type and item["wav"] == file_name:
                return item
        return None
```

</Spoiler>
<Spoiler name="Funkcja główna">
<span>Ciekawostka, że w Pythonie mnożenie tekstów powiela go razy n</span>

<span>
  Sam Python nie wymaga funkcji `main()` do działania, jednak dałem tu jako
  przykład
</span>

```py
def main():
    reader = Reader(source_csv)
    for wav in wavs_path:
        print("Preparing wav for lip gen", wav)
        voice_type = wav.parent.name
        print(f"Searching for text for {wav.name} and {voice_type}")
        item = reader.find_by_file_name(voice_type, wav.name)
        if item is None:
            print(f"No text in {source_csv} for given wav and voice_type. Skipping", end="\n"*2)
            continue
        temp_wav = prepare_wav(wav)
        lip_path = wav.with_name(f"{wav.stem}.lip")
        generate_lip(temp_wav, lip_path, item["text"])
        print("Finished generating lip for", wav)
        # Usuwa plik, tutaj ten pod temp_wav
        temp_wav.unlink()
        print("Removed temp wav", temp_wav, end="\n"*2)

main()
```

</Spoiler>

<Spoiler name="Cały skrypt">

<CodeVariant
  items={[
    { code: fullScriptLinux, lang: 'py', name: 'Linux' },
    { code: fullScriptWindows, lang: 'py', name: 'Windows' },
  ]}
/>

</Spoiler>
</div>

## Podsumowanie

W tym wpisie pokryłem temat generowania samego pliku `.lip`, jednak to nie koniec jego przetwarzania, gdyż jeszcze trzeba go spakować do `.fuz`.

W następny wpisie zamierzam pokryć użytkowanie YakitoAudioConverter oraz fuz_extractor jako specjalizowane narzędzie do obsługi pliku `.fuz`, które można zintegrować z powyższym skryptem.
